import { createTheme } from '@mui/material';

export const theme = createTheme({
  shape: {
    borderRadius: 6,
  },
  typography: {
    fontFamily: "Nunito Sans, -apple-system, BlinkMacSystemFont, Roboto, 'Helvetica Neue', sans-serif",
    fontFamilyCode: 'Menlo,Consolas,"Droid Sans Mono",monospace',
    fontSize: 14,
    htmlFontSize: 16,
  },
});
